export interface IDuration {
  duration: number;
  isDurationUpdatable: boolean;
  isDurationModified: boolean;
}

export interface IDurationRaw {
  duration: number;
  isDurationUpdatable: number;
  isDurationModified: number;
}
